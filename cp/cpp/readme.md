# C++ sieve project

## Quick commands

```bash
make test
make bench
```

## Run tests

```bash
cmake -S . -B build
cmake --build build
ctest --test-dir build --output-on-failure
```

## Run benchmark

```bash
cmake -S . -B build
cmake --build build
```
